version: "3.9"

services:
  payment-service:
    build: .
    container_name: payment-service
    restart: always

    ports:
      - "8005:8005"

    env_file:
      - .env

    mem_limit: 256m
    cpus: 0.5

    networks:
      - dhwaniastro-net

networks:
  dhwaniastro-net:
    external: true
